package chapters.chapter_11.Checkpoints;

public class Checkpoint_17_18_19 {

    /* Polymorphism means that a variable of a supertype can refer to a subtype object.
    * if actual type and refence type isnt same, it means it is dynamic binding.
     JVM decides which method to be invoked through inheritance chain(to upper class each time until it founds) */

    /*Method matching: Declared type decides which method matches. (The declared type of the reference variable decides which method to match at compile time.)
    * Method binding: Actual type decides while running which method to be invoked */

    // we can assign because all classes are a subclass of Object cclass
}
