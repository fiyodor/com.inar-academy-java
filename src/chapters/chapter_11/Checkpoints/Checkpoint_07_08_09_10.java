package chapters.chapter_11.Checkpoints;

public class Checkpoint_07_08_09_10 {

    // False. A private method cannot be reached in subclasses

    //False. if a static method in is tried to override in subclasses it wont work. because method in superclass will be hidden.

    /* we can invoke a constructor from superclass by using keyword 'super'.
     'super()' will invoke no-arg constructor(i.e. default constructor) and super(parameters) will invoke constructors with parameters. */


    /*by using again the keyword 'super'. super.toString() for example

     */
}
