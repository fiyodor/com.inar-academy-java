package chapters.chapter_11.Checkpoints;

public class Checkpoint_11_12_13_14_15_16 {

/*    public class Circle {
        private double radius;

        public Circle(double radius) {
            radius = radius;                            this
        }
        public double getRadius() {
            return radius;
        }
        public double getArea() {
            return radius * radius * Math.PI;
        }
    }
    class B extends Circle {
        private double length;

        B(double radius, double length) {
            Circle(radius);                            super(radius)
            length = length;
        }
        @Override

        public double getArea() {
            return getArea() * length;
        }
    }

 */

 /* in overloading you change the return type and parameters. method name is the same. overloaded methods can be in the same classes.
    in overriding you dont chancge the return type or parameters. when you write an overriden method you say that i dont want to use the method in the superclass. */

    //it is overridden

    // no it wont be a problem.

    // it is overloaded

    //when used and the method doesnt override the method in the superclass a compiler error will occur.
}
