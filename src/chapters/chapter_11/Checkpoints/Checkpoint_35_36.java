/*package chapters.chapter_11.Checkpoints;

public class Checkpoint_35_36 {


        int[] array = {3, 5, 95, 4, 15, 34, 3, 6, 5};
        ArrayList<Integer> list = new ArrayList<>(Arrays.asList(array));

        //array must be an object




        ArrayList<Double> list = new ArrayList<>();
        list.add(1);

        //we must write list.add(1.0); because it is an arraylist of doubles


        int[] array = {3, 5, 95, 4, 15, 34, 3, 6, 5};
        System.out.println(java.util.Collections.max(array));

        //array must be an object. if it was and object then we must use asList(array) method

    }
}
*/