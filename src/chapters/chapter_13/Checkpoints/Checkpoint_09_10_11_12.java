package chapters.chapter_13.Checkpoints;

public class Checkpoint_09_10_11_12 {

    /*No. because Calendar is an abstract class. we must use a class that is a subclass of calendar class
    *
    *add method is abstract
    *
    * we can use Gregorian Calendar no arg constructor, so we can create an instance of Calendar
    *
    *   c.get(Calendar.YEAR)
        c.get(Calendar.MONTH)
        c.get(Calendar.DAY_OF_MONTH)
        c.get(Calendar.HOUR)
        c.get(Calendar.MINUTE)
        c.get(Calendar.SECOND) */
}
