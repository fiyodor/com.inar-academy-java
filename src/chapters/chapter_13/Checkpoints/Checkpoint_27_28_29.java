package chapters.chapter_13.Checkpoints;

public class Checkpoint_27_28_29 {

    /*java permits a class to inherit one class but a class can implement multiple interfaces
    *
    *abstract classes have no variable restrictions, cant be invoked using new, have no method restrictions
    * interfaces' variables must be public static final, have no constructors and cant be instantiated usin new, all meyhods must be public abstract instance methods
    *
    *   a. An interface is compiled into a separate bytecode file.        true
        b. An interface can have static methods.                          true
        c. An interface can extend one or more interfaces.                true
        d. An interface can extend an abstract class.                     false
        e. An abstract class can extend an interface.                     true  */
}
